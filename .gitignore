node_modules/
.next/
dist/
build/
out/

.env
.env.local
.env*.local

.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*

coverage/
.turbo/
.vercel/
*.tsbuildinfo

# Local image mirror produced by the importer before Blob is configured
apps/web/public/media/

# Vercel CLI writes .env.local (OIDC token) and appends a broad .env* rule.
# Keep the broad rule, but the committed example must stay tracked.
.env*
!.env.example
